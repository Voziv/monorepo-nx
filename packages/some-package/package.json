{
  "name": "some-package",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "main": "src/some-package.ts",
  "scripts": {
    "build": "npm run-script clean && tsc",
    "clean": "rimraf dist/",
    "lint": "npx eslint src/",
    "lint:fix": "npm run lint -- --fix",
    "test": "jest"
  }
}
